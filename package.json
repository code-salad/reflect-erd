{
  "name": "vsequel",
  "version": "0.1.8",
  "description": "A CLI tool for extracting database schemas and generating ERD diagrams from PostgreSQL and MySQL databases",
  "keywords": [
    "erd",
    "database",
    "schema",
    "plantuml",
    "postgresql",
    "mysql",
    "cli"
  ],
  "maintainers": [
    "vikyw89<vikyw89@gmail.com>"
  ],
  "homepage": "https://github.com/code-salad/vsequel#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/code-salad/vsequel.git"
  },
  "scripts": {
    "encrypt": "dotenvx encrypt",
    "decrypt": "dotenvx decrypt",
    "format": "npx ultracite format",
    "lint": "npx ultracite lint",
    "seed": "npx tsx seeds/setup.ts",
    "build": "rm -rf dist && npx tsc",
    "prepublishOnly": "npm run build",
    "test": "npx tsx --test tests/**/*.spec.ts",
    "test:ee": "docker compose -f tests/docker-compose.yml up -d --wait && npm run seed && npm test && docker compose -f tests/docker-compose.yml down",
    "test:docker": "docker compose -f tests/docker-compose.yml up -d --wait && npx tsx seeds/setup.ts && npx tsx --test tests/docker-integration.spec.ts && docker compose -f tests/docker-compose.yml down"
  },
  "license": "MIT",
  "author": "Reflect",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "vsequel": "dist/cli/index.js"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/**/*.js.map",
    "src/**/*.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "mysql2": "^3.14.3",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@dotenvx/dotenvx": "^1.48.4",
    "@tsconfig/recommended": "^1.0.10",
    "@types/node": "^24.2.1",
    "lefthook": "^1.12.2",
    "tsx": "^4.20.5",
    "ultracite": "5.1.2",
    "zod": "^3.24.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  }
}
